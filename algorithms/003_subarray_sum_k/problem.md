# Problem 003: Count Subarrays With Sum = K

You are given an integer array `nums` and an integer `k`.

A **subarray** is a contiguous non-empty sequence of elements in `nums`.

Your task is to return the **number of subarrays** whose elements sum exactly to `k`.

### Function Signature

```python
from typing import List

def count_subarrays_with_sum(nums: List[int], k: int) -> int:
    ...
